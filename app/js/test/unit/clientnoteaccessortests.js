describe("client note accessor", function() {

	beforeEach(module('noteApp'));

	describe("find", function() {

		it("should return a single record for id = 1", function(done) {
			assert.equal(true, true);			
			done();
		});

	});

});